# Copy to .env and fill in. Never commit the real .env.
# Supabase project = Sales Coach (tgfoylcsdxqfmyfcnrkc).
SUPABASE_URL=https://tgfoylcsdxqfmyfcnrkc.supabase.co

# service_role key: Supabase Dashboard > Project Settings > API > service_role.
# Secret — bypasses row security. Lives only here on the host, never in git/chat.
SUPABASE_SERVICE_KEY=

# Optional: seconds between Supabase polls (default 4).
POLL_INTERVAL=4
